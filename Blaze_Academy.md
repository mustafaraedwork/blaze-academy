# 🔥 Blaze Academy - وثيقة المشروع الشاملة

## 📋 نظرة عامة على المشروع

**اسم المشروع:** Blaze Academy  
**اسم الكورس:** ميتا كما لم تراها من قبل  
**الوصف:** منصة تعليمية لكورس التسويق على ميتا وتشغيل الإعلانات مع نظام اشتراكات مدفوع

---

## 🛠️ التقنيات المستخدمة

### Frontend:
- **React.js** (Latest Version)
- **React Router** للتنقل بين الصفحات
- **Tailwind CSS** للتصميم
- **React Context API** أو **Redux** لإدارة الحالة
- **Axios** للاتصال بالـ API

### Backend & Database:
- **Supabase** (Backend as a Service)
  - المصادقة والتوثيق (Authentication)
  - قاعدة البيانات (PostgreSQL)
  - تخزين الملفات (Storage)
  - Real-time subscriptions

### Hosting & Deployment:
- **Vercel** لاستضافة الموقع
- **Supabase Cloud** للباك إند

### إضافات:
- **React Player** لتشغيل الفيديوهات
- **React Icons** للأيقونات
- **React Toastify** للإشعارات
- **Formik + Yup** للنماذج والتحقق

---

## 🎨 التصميم والألوان

### نظام الألوان:

#### الوضع الداكن (Dark Mode):
- **الخلفية الرئيسية:** `#0A0A0A` (أسود)
- **الخلفية الثانوية:** `#1A1A1A` (رمادي داكن)
- **اللون الأساسي:** `#FF6B35` (برتقالي)
- **اللون الثانوي:** `#FF8C42` (برتقالي فاتح)
- **النص الرئيسي:** `#FFFFFF` (أبيض)
- **النص الثانوي:** `#B0B0B0` (رمادي فاتح)

#### الوضع الفاتح (Light Mode):
- **الخلفية الرئيسية:** `#FFFFFF` (أبيض)
- **الخلفية الثانوية:** `#F5F5F5` (رمادي فاتح جداً)
- **اللون الأساسي:** `#FF6B35` (برتقالي)
- **اللون الثانوي:** `#FF8C42` (برتقالي فاتح)
- **النص الرئيسي:** `#1A1A1A` (أسود)
- **النص الثانوي:** `#666666` (رمادي)

### خط الكتابة:
- **العربية:** Cairo, Tajawal
- **الإنجليزية:** Inter, Roboto

---

## 📊 هيكل قاعدة البيانات (Supabase Schema)

### 1. جدول المستخدمين (users)
```sql
CREATE TABLE users (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  email TEXT UNIQUE NOT NULL,
  full_name TEXT NOT NULL,
  avatar_url TEXT,
  is_subscribed BOOLEAN DEFAULT FALSE,
  subscription_date TIMESTAMP,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
```

### 2. جدول الأقسام (sections)
```sql
CREATE TABLE sections (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  title TEXT NOT NULL,
  description TEXT,
  order_number INTEGER NOT NULL,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
```

### 3. جدول الدروس (lessons)
```sql
CREATE TABLE lessons (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  section_id UUID REFERENCES sections(id) ON DELETE CASCADE,
  title TEXT NOT NULL,
  description TEXT,
  video_url TEXT NOT NULL,
  duration_minutes INTEGER,
  is_free BOOLEAN DEFAULT FALSE,
  notes TEXT,
  order_number INTEGER NOT NULL,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
```

### 4. جدول الملفات المرفقة (lesson_attachments)
```sql
CREATE TABLE lesson_attachments (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  lesson_id UUID REFERENCES lessons(id) ON DELETE CASCADE,
  file_name TEXT NOT NULL,
  file_url TEXT NOT NULL,
  file_type TEXT NOT NULL,
  file_size INTEGER,
  created_at TIMESTAMP DEFAULT NOW()
);
```

### 5. جدول التقدم (user_progress)
```sql
CREATE TABLE user_progress (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID REFERENCES users(id) ON DELETE CASCADE,
  lesson_id UUID REFERENCES lessons(id) ON DELETE CASCADE,
  completed BOOLEAN DEFAULT FALSE,
  completed_at TIMESTAMP,
  last_watched_position INTEGER DEFAULT 0,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW(),
  UNIQUE(user_id, lesson_id)
);
```

### 6. جدول الكوبونات (coupons)
```sql
CREATE TABLE coupons (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  code TEXT UNIQUE NOT NULL,
  discount_type TEXT NOT NULL, -- 'percentage' or 'fixed'
  discount_value NUMERIC NOT NULL,
  max_uses INTEGER NOT NULL,
  current_uses INTEGER DEFAULT 0,
  is_active BOOLEAN DEFAULT TRUE,
  expires_at TIMESTAMP,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
```

### 7. جدول استخدام الكوبونات (coupon_usage)
```sql
CREATE TABLE coupon_usage (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  coupon_id UUID REFERENCES coupons(id) ON DELETE CASCADE,
  user_id UUID REFERENCES users(id) ON DELETE CASCADE,
  used_at TIMESTAMP DEFAULT NOW(),
  UNIQUE(coupon_id, user_id)
);
```

### 8. جدول الاشتراكات (subscriptions)
```sql
CREATE TABLE subscriptions (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID REFERENCES users(id) ON DELETE CASCADE,
  amount_paid NUMERIC NOT NULL,
  coupon_code TEXT,
  discount_amount NUMERIC DEFAULT 0,
  payment_status TEXT NOT NULL, -- 'pending', 'completed', 'failed'
  payment_method TEXT,
  created_at TIMESTAMP DEFAULT NOW()
);
```

---

## 🗂️ هيكل المشروع (Project Structure)

```
blaze-academy/
├── public/
│   ├── index.html
│   ├── favicon.ico
│   └── assets/
│       ├── images/
│       └── videos/
├── src/
│   ├── components/
│   │   ├── common/
│   │   │   ├── Navbar.jsx
│   │   │   ├── Footer.jsx
│   │   │   ├── Button.jsx
│   │   │   ├── Input.jsx
│   │   │   ├── Modal.jsx
│   │   │   └── Loader.jsx
│   │   ├── auth/
│   │   │   ├── LoginForm.jsx
│   │   │   ├── RegisterForm.jsx
│   │   │   └── ProtectedRoute.jsx
│   │   ├── course/
│   │   │   ├── SectionCard.jsx
│   │   │   ├── LessonCard.jsx
│   │   │   ├── VideoPlayer.jsx
│   │   │   └── ProgressBar.jsx
│   │   ├── payment/
│   │   │   ├── PricingCard.jsx
│   │   │   ├── CouponInput.jsx
│   │   │   └── PaymentForm.jsx
│   │   └── admin/
│   │       ├── SectionManager.jsx
│   │       ├── LessonManager.jsx
│   │       ├── CouponManager.jsx
│   │       └── UserManagement.jsx
│   ├── pages/
│   │   ├── LandingPage.jsx
│   │   ├── LoginPage.jsx
│   │   ├── RegisterPage.jsx
│   │   ├── CoursePage.jsx
│   │   ├── LessonPage.jsx
│   │   ├── PricingPage.jsx
│   │   ├── ProfilePage.jsx
│   │   ├── AdminDashboard.jsx
│   │   └── NotFoundPage.jsx
│   ├── context/
│   │   ├── AuthContext.jsx
│   │   ├── ThemeContext.jsx
│   │   └── CourseContext.jsx
│   ├── services/
│   │   ├── supabaseClient.js
│   │   ├── authService.js
│   │   ├── courseService.js
│   │   ├── paymentService.js
│   │   └── storageService.js
│   ├── hooks/
│   │   ├── useAuth.js
│   │   ├── useTheme.js
│   │   └── useCourse.js
│   ├── utils/
│   │   ├── constants.js
│   │   ├── helpers.js
│   │   └── validators.js
│   ├── styles/
│   │   ├── globals.css
│   │   └── tailwind.css
│   ├── App.jsx
│   ├── index.js
│   └── routes.js
├── .env
├── .gitignore
├── package.json
├── tailwind.config.js
├── postcss.config.js
├── vercel.json
└── README.md
```

---

## 📄 الصفحات والمميزات التفصيلية

### 1. 🏠 صفحة الهبوط (Landing Page)

#### المحتوى:
- **Hero Section:**
  - عنوان جذاب: "ميتا كما لم تراها من قبل"
  - وصف مختصر للكورس
  - زر CTA: "ابدأ التعلم الآن"
  - صورة أو فيديو توضيحي

- **مميزات الكورس:**
  - قائمة بأبرز ما يقدمه الكورس
  - أيقونات جذابة

- **معاينة المحتوى:**
  - عرض سريع للأقسام (بدون تفاصيل)
  - عدد الدروس والمدة الإجمالية

- **التسعير:**
  - السعر: 99,000 دينار عراقي
  - زر الاشتراك

- **شهادات الطلاب:**
  - آراء وتقييمات (إن وجدت)

- **عن المدرب:**
  - نبذة عن Blaze Academy

- **FAQ:**
  - أسئلة شائعة

#### المميزات التقنية:
- ✅ Smooth Scrolling
- ✅ Animations (AOS Library)
- ✅ Responsive Design
- ✅ Dark/Light Mode Toggle

---

### 2. 🔐 صفحة التسجيل (Register Page)

#### الحقول المطلوبة:
- الاسم الكامل
- البريد الإلكتروني
- كلمة المرور
- تأكيد كلمة المرور
- موافقة على الشروط والأحكام

#### التحقق (Validation):
- البريد الإلكتروني صحيح
- كلمة المرور قوية (8 أحرف على الأقل)
- تطابق كلمات المرور

#### بعد التسجيل:
- إرسال بريد تأكيد (Supabase Auth)
- توجيه المستخدم لصفحة الكورسات
- حساب غير مشترك (Free Tier)

---

### 3. 🔑 صفحة تسجيل الدخول (Login Page)

#### الحقول:
- البريد الإلكتروني
- كلمة المرور
- "تذكرني"
- رابط "نسيت كلمة المرور؟"

#### المميزات:
- تسجيل دخول عبر Supabase Auth
- رسائل خطأ واضحة
- إعادة توجيه بعد الدخول

---

### 4. 📚 صفحة الكورسات (Course Page)

#### التصميم:
```
┌─────────────────────────────────────────┐
│  📚 ميتا كما لم تراها من قبل          │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│  التقدم الكلي: ████░░░░░░░ 35%        │
└─────────────────────────────────────────┘

▼ 📖 القسم 1: الأساسيات (4 دروس - 45 دقيقة)
  ✅ الدرس 1: مقدمة عن التسويق (10 د) 🆓
  🔒 الدرس 2: فهم منصة ميتا (12 د)
  🔒 الدرس 3: أنواع الإعلانات (15 د)
  🔒 الدرس 4: المصطلحات المهمة (8 د)

▶ 📖 القسم 2: إنشاء الحساب (5 دروس - 60 دقيقة) 🔒

▶ 📖 القسم 3: إطلاق الحملات (8 دروس - 90 دقيقة) 🔒
```

#### المميزات:
- ✅ عرض تسلسلي للأقسام
- ✅ Accordion (فتح/إغلاق الأقسام)
- ✅ أيقونة قفل للدروس المغلقة
- ✅ علامة "مجاني" على الدرس الأول
- ✅ عند الضغط على درس مقفل → Modal يطلب الاشتراك
- ✅ شريط تقدم لكل قسم
- ✅ شريط تقدم عام

---

### 5. 🎬 صفحة الدرس (Lesson Page)

#### التصميم:
```
┌──────────────────────────────────────────┐
│                                          │
│         🎬 مشغل الفيديو                 │
│                                          │
└──────────────────────────────────────────┘

📝 عنوان الدرس: مقدمة عن التسويق الرقمي

📄 الوصف:
في هذا الدرس سنتعرف على أساسيات التسويق...

📎 الملفات المرفقة:
┌────────────────────────────────────┐
│ 📄 ملف-الكود.html      [📥 تحميل] │
│ 📊 جدول-التتبع.xlsx    [📥 تحميل] │
│ 📋 ملاحظات-الدرس.pdf  [📥 تحميل] │
└────────────────────────────────────┘

💡 ملاحظات إضافية:
[نص الملاحظات هنا...]

┌────────────────────────────────────┐
│  ⬅️ الدرس السابق | الدرس التالي ➡️ │
│        ✅ [تم إنهاء الدرس]        │
└────────────────────────────────────┘
```

#### المميزات:
- ✅ مشغل فيديو متقدم (تشغيل، إيقاف، الوضع الكامل)
- ✅ حفظ موضع المشاهدة
- ✅ عرض الوصف والملاحظات
- ✅ تحميل الملفات المرفقة
- ✅ زر "تم الإنهاء" لتسجيل التقدم
- ✅ التنقل بين الدروس

---

### 6. 💳 صفحة الدفع والتسعير (Pricing Page)

#### التصميم:
```
┌─────────────────────────────────────┐
│      🔥 اشترك الآن في الكورس       │
├─────────────────────────────────────┤
│                                     │
│  ✨ احصل على وصول كامل إلى:        │
│  ✓ جميع الدروس والأقسام           │
│  ✓ الملفات والموارد القابلة للتحميل│
│  ✓ شهادة إتمام                     │
│  ✓ دعم فني مباشر                   │
│                                     │
├─────────────────────────────────────┤
│                                     │
│  السعر الأصلي: 99,000 د.ع          │
│                                     │
│  🎟️ لديك كود خصم؟                  │
│  [____________] [تطبيق]             │
│                                     │
│  ✅ تم تطبيق: BLAZE30               │
│  الخصم: -29,700 د.ع (30%)          │
│  ─────────────────────────          │
│  💰 المجموع: 69,300 د.ع            │
│                                     │
│  📧 البريد الإلكتروني:              │
│  [user@email.com]                   │
│                                     │
│  💳 طريقة الدفع:                    │
│  ○ بطاقة ائتمان                    │
│  ○ ZainCash                        │
│  ○ تحويل بنكي                      │
│                                     │
│  [✓] أوافق على الشروط والأحكام     │
│                                     │
│       [🔒 إتمام عملية الدفع]       │
│                                     │
└─────────────────────────────────────┘
```

#### نظام الكوبونات:
- حقل إدخال الكود
- زر "تطبيق"
- التحقق من:
  - صحة الكود
  - عدد الاستخدامات المتبقية
  - تاريخ الانتهاء
  - عدم استخدامه من قبل نفس المستخدم

#### رسائل الخطأ:
- ❌ "الكود غير صحيح"
- ❌ "الكود منتهي الصلاحية"
- ❌ "تم استنفاد جميع الاستخدامات"
- ❌ "تم استخدام هذا الكود مسبقاً"

#### بعد الدفع:
- تحديث حالة المستخدم: `is_subscribed = true`
- إرسال بريد تأكيد
- فتح جميع الدروس
- توجيه لصفحة الكورسات

---

### 7. 👤 صفحة الملف الشخصي (Profile Page)

#### المحتوى:
```
┌─────────────────────────────────────┐
│  👤 أحمد محمد                       │
│  📧 ahmed@email.com                 │
│  📅 عضو منذ: 15 أكتوبر 2025        │
│  ✨ الحالة: مشترك                   │
└─────────────────────────────────────┘

📊 التقدم الكلي في الكورس
████████░░░░░░░░ 45%

┌─────────────────────────────────────┐
│  📈 إحصائياتي                       │
├─────────────────────────────────────┤
│  ✅ دروس مكتملة: 15 / 32            │
│  ⏱️ وقت المشاهدة: 8 ساعات          │
│  🔥 أيام متتالية: 7 أيام            │
└─────────────────────────────────────┘

▼ 📖 تقدم الأقسام
  ✅ القسم 1: الأساسيات - مكتمل 100%
  🔄 القسم 2: إنشاء الحساب - 60%
  🔒 القسم 3: إطلاق الحملات - 0%
  🔒 القسم 4: التحليل - 0%

┌─────────────────────────────────────┐
│  🎓 الشهادة                         │
├─────────────────────────────────────┤
│  أكمل الكورس للحصول على شهادتك!   │
│  ████████░░░░░░░░ 45%               │
└─────────────────────────────────────┘

[⚙️ إعدادات الحساب] [🚪 تسجيل الخروج]
```

#### المميزات:
- ✅ معلومات المستخدم
- ✅ حالة الاشتراك
- ✅ شريط التقدم العام
- ✅ تقدم كل قسم
- ✅ إحصائيات التعلم
- ✅ شهادة الإتمام (عند 100%)
- ✅ إعدادات الحساب

---

### 8. 🎛️ لوحة التحكم - Admin Dashboard

#### الصفحة الرئيسية (Dashboard Home):
```
┌─────────────────────────────────────┐
│  📊 الإحصائيات السريعة              │
├─────────────────────────────────────┤
│  👥 المستخدمين: 1,245              │
│  💰 المشتركين: 856                 │
│  📚 الأقسام: 6                      │
│  🎬 الدروس: 42                      │
│  🎟️ الكوبونات النشطة: 3            │
└─────────────────────────────────────┘
```

#### القائمة الجانبية:
- 🏠 لوحة التحكم
- 📚 إدارة الأقسام
- 🎬 إدارة الدروس
- 👥 إدارة المستخدمين
- 🎟️ إدارة الكوبونات
- 📊 التقارير
- ⚙️ الإعدادات

---

#### 📚 إدارة الأقسام:

```
┌─────────────────────────────────────┐
│  ➕ إضافة قسم جديد                  │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  📖 القسم 1: الأساسيات              │
│  📝 الوصف: مقدمة شاملة...          │
│  🎬 عدد الدروس: 4                   │
│  ⏱️ المدة: 45 دقيقة                 │
│  [✏️ تعديل] [🗑️ حذف] [↑] [↓]      │
├─────────────────────────────────────┤
│  📖 القسم 2: إنشاء الحساب          │
│  📝 الوصف: خطوات إنشاء...          │
│  🎬 عدد الدروس: 5                   │
│  ⏱️ المدة: 60 دقيقة                 │
│  [✏️] [🗑️] [↑] [↓]                 │
└─────────────────────────────────────┘
```

##### نموذج إضافة قسم:
```
┌─ إضافة قسم جديد ───────────────┐
│                                 │
│ 📝 عنوان القسم: *              │
│ [_________________________]    │
│                                 │
│ 📄 وصف القسم:                  │
│ [_________________________]    │
│ [_________________________]    │
│                                 │
│ 🔢 ترتيب القسم: *              │
│ [___]                           │
│                                 │
│    [إلغاء]  [✅ حفظ القسم]     │
└─────────────────────────────────┘
```

---

#### 🎬 إدارة الدروس:

```
┌─────────────────────────────────────┐
│  📖 القسم: [اختر القسم ▼]          │
│  ➕ إضافة درس جديد                  │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  🎬 الدرس 1: مقدمة عن التسويق     │
│  ⏱️ المدة: 10 دقائق                │
│  🆓 مجاني: نعم                      │
│  📎 مرفقات: 2 ملفات                │
│  [▶️ معاينة] [✏️] [🗑️] [↑] [↓]    │
├─────────────────────────────────────┤
│  🎬 الدرس 2: فهم منصة ميتا         │
│  ⏱️ المدة: 12 دقيقة                │
│  🔒 مدفوع                            │
│  📎 مرفقات: 1 ملف                  │
│  [▶️] [✏️] [🗑️] [↑] [↓]           │
└─────────────────────────────────────┘
```

##### نموذج إضافة درس:
```
┌─ إضافة درس جديد ───────────────┐
│                                 │
│ 📖 اختر القسم: *                │
│ [القسم 1: الأساسيات ▼]         │
│                                 │
│ 📝 عنوان الدرس: *              │
│ [_________________________]    │
│                                 │
│ 📄 وصف الدرس:                  │
│ [_________________________]    │
│ [_________________________]    │
│                                 │
│ 🎬 رابط الفيديو / رفع: *       │
│ [_________________________]    │
│ [📤 أو اختر من الجهاز]         │
│                                 │
│ ⏱️ مدة الفيديو (بالدقائق): *   │
│ [___]                           │
│                                 │
│ 🆓 درس مجاني؟                  │
│ [✓] نعم  [ ] لا                 │
│                                 │
│ 📎 الملفات المرفقة:            │
│ [📤 إضافة ملفات]               │
│ ┌─────────────────────────┐    │
│ │ - كود.html      [🗑️]   │    │
│ │ - ملاحظات.pdf  [🗑️]   │    │
│ └─────────────────────────┘    │
│                                 │
│ 💡 ملاحظات إضافية:             │
│ [_________________________]    │
│ [_________________________]    │
│                                 │
│ 🔢 ترتيب الدرس:                │
│ [___]                           │
│                                 │
│    [إلغاء]  [✅ حفظ الدرس]     │
└─────────────────────────────────┘
```

---

#### 🎟️ إدارة الكوبونات:

```
┌─────────────────────────────────────┐
│  ➕ إنشاء كوبون جديد                │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  🎟️ BLAZE30                         │
│  💰 خصم: 30%                         │
│  👥 الاستخدامات: 7/10               │
│  📅 ينتهي: 2025-12-31                │
│  ✅ نشط                              │
│  [✏️ تعديل] [❌ تعطيل] [🗑️ حذف]   │
├─────────────────────────────────────┤
│  🎟️ SPECIAL50                       │
│  💰 خصم: 50%                         │
│  👥 الاستخدامات: 5/5                │
│  📅 ينتهي: 2025-11-30                │
│  🔴 مكتمل                            │
│  [✏️] [🗑️]                          │
└─────────────────────────────────────┘
```

##### نموذج إنشاء كوبون:
```
┌─ إنشاء كوبون خصم ──────────────┐
│                                 │
│ 🏷️ كود الكوبون: *              │
│ [BLAZE____________]             │
│                                 │
│ 💰 نوع الخصم: *                │
│ ⚫ نسبة مئوية (%)               │
│ ○ مبلغ ثابت (د.ع)              │
│                                 │
│ 📊 قيمة الخصم: *               │
│ [30] %                          │
│                                 │
│ 👥 عدد الاستخدامات: *          │
│ [10] شخص                        │
│                                 │
│ 📅 تاريخ الانتهاء:             │
│ [2025-12-31]                    │
│                                 │
│ ✅ نشط عند الإنشاء             │
│ [✓] نعم                         │
│                                 │
│   [إلغاء]  [✅ إنشاء الكوبون]  │
└─────────────────────────────────┘
```

---

#### 👥 إدارة المستخدمين:

```
┌─────────────────────────────────────┐
│  🔍 [بحث عن مستخدم...]             │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  👤 أحمد محمد                       │
│  📧 ahmed@email.com                 │
│  ✨ مشترك | 📅 15 أكتوبر 2025     │
│  📊 التقدم: 45%                     │
│  [👁️ عرض] [✏️] [🚫 حظر]           │
├─────────────────────────────────────┤
│  👤 سارة علي                        │
│  📧 sara@email.com                  │
│  ⭕ غير مشترك | 📅 20 أكتوبر 2025 │
│  📊 التقدم: 5%                      │
│  [👁️] [✏️] [💰 إضافة اشتراك]      │
└─────────────────────────────────────┘
```

---

## 🔐 نظام الصلاحيات والحماية

### أنواع المستخدمين:
1. **Admin (مدير):**
   - الوصول الكامل لجميع الصفحات
   - إدارة المحتوى والمستخدمين

2. **Subscribed User (مستخدم مشترك):**
   - الوصول لجميع الدروس
   - متابعة التقدم
   - تحميل الملفات

3. **Free User (مستخدم مجاني):**
   - الوصول للدرس المجاني فقط
   - عرض قائمة الكورس مع القفل

### Row Level Security (RLS) في Supabase:

#### للدروس:
```sql
-- المستخدمون المشتركون فقط يمكنهم الوصول للدروس المدفوعة
CREATE POLICY "subscribed_users_lessons"
ON lessons FOR SELECT
USING (
  is_free = true OR 
  EXISTS (
    SELECT 1 FROM users 
    WHERE users.id = auth.uid() 
    AND users.is_subscribed = true
  )
);
```

#### للتقدم:
```sql
-- المستخدم يمكنه فقط رؤية وتعديل تقدمه الخاص
CREATE POLICY "user_own_progress"
ON user_progress FOR ALL
USING (user_id = auth.uid())
WITH CHECK (user_id = auth.uid());
```

#### للأدمن:
```sql
-- فقط الأدمن يمكنه إدارة المحتوى
CREATE POLICY "admin_full_access"
ON sections FOR ALL
USING (
  EXISTS (
    SELECT 1 FROM users
    WHERE users.id = auth.uid()
    AND users.role = 'admin'
  )
);
```

---

## 📁 تخزين الملفات (Supabase Storage)

### المجلدات (Buckets):

1. **videos/**
   - تخزين ملفات الفيديو
   - الحد الأقصى: 500MB لكل ملف
   - الصيغ: MP4, WEBM, MOV

2. **attachments/**
   - ملفات الدروس (كود، PDF، إلخ)
   - الحد الأقصى: 50MB لكل ملف
   - الصيغ: PDF, HTML, CSS, JS, XLSX, ZIP

3. **avatars/**
   - صور المستخدمين
   - الحد الأقصى: 2MB
   - الصيغ: JPG, PNG, WEBP

### سياسات الوصول (Storage Policies):

```sql
-- السماح للجميع بقراءة الفيديوهات المجانية
CREATE POLICY "public_videos"
ON storage.objects FOR SELECT
USING (bucket_id = 'videos' AND metadata->>'is_free' = 'true');

-- فقط المشتركون يمكنهم الوصول للفيديوهات المدفوعة
CREATE POLICY "subscribed_videos"
ON storage.objects FOR SELECT
USING (
  bucket_id = 'videos' AND
  EXISTS (
    SELECT 1 FROM users
    WHERE users.id = auth.uid()
    AND users.is_subscribed = true
  )
);

-- فقط الأدمن يمكنه رفع الملفات
CREATE POLICY "admin_upload"
ON storage.objects FOR INSERT
WITH CHECK (
  EXISTS (
    SELECT 1 FROM users
    WHERE users.id = auth.uid()
    AND users.role = 'admin'
  )
);
```

---

## 🔄 تدفق المستخدم (User Flow)

### 1. مستخدم جديد (غير مسجل):
```
1. زيارة Landing Page
   ↓
2. معاينة محتوى الكورس
   ↓
3. الضغط على "ابدأ الآن" أو "اشترك"
   ↓
4. صفحة التسجيل (Register)
   ↓
5. إدخال البيانات + التحقق من البريد
   ↓
6. توجيه لصفحة الكورسات (Free Access)
   ↓
7. مشاهدة الدرس المجاني
   ↓
8. محاولة فتح درس مقفل → Modal "يجب الاشتراك"
   ↓
9. الضغط على "اشترك الآن"
   ↓
10. صفحة الدفع
    ↓
11. إدخال الكوبون (اختياري)
    ↓
12. إتمام الدفع
    ↓
13. تحديث الحالة → Subscribed
    ↓
14. فتح جميع الدروس
```

### 2. مستخدم مشترك:
```
1. تسجيل الدخول
   ↓
2. صفحة الكورسات (جميع الدروس مفتوحة)
   ↓
3. اختيار درس
   ↓
4. مشاهدة الفيديو + تحميل المرفقات
   ↓
5. الضغط على "تم الإنهاء"
   ↓
6. تحديث التقدم (Progress)
   ↓
7. الانتقال للدرس التالي
   ↓
8. عند إتمام 100% → شهادة الإتمام
```

### 3. الأدمن:
```
1. تسجيل الدخول (admin@blaze.com)
   ↓
2. Admin Dashboard
   ↓
3. إضافة قسم جديد
   ↓
4. إضافة دروس للقسم
   ↓
5. رفع فيديوهات + مرفقات
   ↓
6. إنشاء كوبونات خصم
   ↓
7. متابعة المستخدمين والاشتراكات
```

---

## 🚀 خطوات التنفيذ (Implementation Steps)

### المرحلة 1: الإعداد الأولي (Setup)

#### 1. إنشاء مشروع React:
```bash
npx create-react-app blaze-academy
cd blaze-academy
```

#### 2. تثبيت المكتبات:
```bash
# Core Dependencies
npm install react-router-dom axios

# UI Libraries
npm install tailwindcss postcss autoprefixer
npm install react-icons
npm install react-toastify

# Supabase
npm install @supabase/supabase-js

# Form Handling
npm install formik yup

# Video Player
npm install react-player

# Optional
npm install aos # Animations
npm install recharts # Charts for admin
```

#### 3. إعداد Tailwind CSS:
```bash
npx tailwindcss init -p
```

**tailwind.config.js:**
```javascript
module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}",
  ],
  darkMode: 'class',
  theme: {
    extend: {
      colors: {
        primary: {
          light: '#FF8C42',
          DEFAULT: '#FF6B35',
        },
        dark: {
          bg: '#0A0A0A',
          secondary: '#1A1A1A',
          text: '#B0B0B0',
        },
        light: {
          bg: '#FFFFFF',
          secondary: '#F5F5F5',
          text: '#666666',
        }
      },
      fontFamily: {
        arabic: ['Cairo', 'sans-serif'],
        english: ['Inter', 'sans-serif'],
      }
    },
  },
  plugins: [],
}
```

#### 4. إعداد Supabase:

**إنشاء المشروع:**
1. الذهاب إلى [supabase.com](https://supabase.com)
2. إنشاء حساب جديد
3. إنشاء مشروع جديد: "blaze-academy"
4. نسخ API URL و API Key

**ملف .env:**
```env
REACT_APP_SUPABASE_URL=your_supabase_url
REACT_APP_SUPABASE_ANON_KEY=your_supabase_anon_key
```

**src/services/supabaseClient.js:**
```javascript
import { createClient } from '@supabase/supabase-js';

const supabaseUrl = process.env.REACT_APP_SUPABASE_URL;
const supabaseAnonKey = process.env.REACT_APP_SUPABASE_ANON_KEY;

export const supabase = createClient(supabaseUrl, supabaseAnonKey);
```

---

### المرحلة 2: إنشاء قاعدة البيانات

#### تنفيذ SQL في Supabase:

1. الذهاب إلى **SQL Editor** في Supabase
2. تنفيذ جميع الجداول من قسم "هيكل قاعدة البيانات"
3. إنشاء Storage Buckets:
   - videos
   - attachments
   - avatars
4. تطبيق Row Level Security policies

---

### المرحلة 3: بناء المكونات الأساسية

#### 1. نظام التوثيق (Auth System):

**src/context/AuthContext.jsx:**
```javascript
import React, { createContext, useState, useEffect, useContext } from 'react';
import { supabase } from '../services/supabaseClient';

const AuthContext = createContext();

export const AuthProvider = ({ children }) => {
  const [user, setUser] = useState(null);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    // Check active session
    supabase.auth.getSession().then(({ data: { session } }) => {
      setUser(session?.user ?? null);
      setLoading(false);
    });

    // Listen for auth changes
    const { data: { subscription } } = supabase.auth.onAuthStateChange(
      (_event, session) => {
        setUser(session?.user ?? null);
      }
    );

    return () => subscription.unsubscribe();
  }, []);

  const signUp = async (email, password, fullName) => {
    const { data, error } = await supabase.auth.signUp({
      email,
      password,
      options: {
        data: {
          full_name: fullName,
        }
      }
    });
    return { data, error };
  };

  const signIn = async (email, password) => {
    const { data, error } = await supabase.auth.signInWithPassword({
      email,
      password,
    });
    return { data, error };
  };

  const signOut = async () => {
    const { error } = await supabase.auth.signOut();
    return { error };
  };

  const value = {
    user,
    signUp,
    signIn,
    signOut,
    loading,
  };

  return (
    <AuthContext.Provider value={value}>
      {!loading && children}
    </AuthContext.Provider>
  );
};

export const useAuth = () => {
  return useContext(AuthContext);
};
```

---

#### 2. نظام الثيمات (Theme System):

**src/context/ThemeContext.jsx:**
```javascript
import React, { createContext, useState, useEffect, useContext } from 'react';

const ThemeContext = createContext();

export const ThemeProvider = ({ children }) => {
  const [isDark, setIsDark] = useState(() => {
    const saved = localStorage.getItem('theme');
    return saved === 'dark' || (!saved && window.matchMedia('(prefers-color-scheme: dark)').matches);
  });

  useEffect(() => {
    if (isDark) {
      document.documentElement.classList.add('dark');
      localStorage.setItem('theme', 'dark');
    } else {
      document.documentElement.classList.remove('dark');
      localStorage.setItem('theme', 'light');
    }
  }, [isDark]);

  const toggleTheme = () => {
    setIsDark(!isDark);
  };

  return (
    <ThemeContext.Provider value={{ isDark, toggleTheme }}>
      {children}
    </ThemeContext.Provider>
  );
};

export const useTheme = () => {
  return useContext(ThemeContext);
};
```

---

#### 3. خدمات الكورس (Course Services):

**src/services/courseService.js:**
```javascript
import { supabase } from './supabaseClient';

// Get all sections with lessons
export const getSections = async () => {
  const { data, error } = await supabase
    .from('sections')
    .select(`
      *,
      lessons (
        *,
        lesson_attachments (*)
      )
    `)
    .order('order_number', { ascending: true });
  
  return { data, error };
};

// Get single lesson
export const getLesson = async (lessonId) => {
  const { data, error } = await supabase
    .from('lessons')
    .select(`
      *,
      section:sections(*),
      lesson_attachments(*)
    `)
    .eq('id', lessonId)
    .single();
  
  return { data, error };
};

// Mark lesson as completed
export const markLessonComplete = async (userId, lessonId) => {
  const { data, error } = await supabase
    .from('user_progress')
    .upsert({
      user_id: userId,
      lesson_id: lessonId,
      completed: true,
      completed_at: new Date().toISOString(),
    });
  
  return { data, error };
};

// Get user progress
export const getUserProgress = async (userId) => {
  const { data, error } = await supabase
    .from('user_progress')
    .select('*')
    .eq('user_id', userId);
  
  return { data, error };
};

// Admin: Create section
export const createSection = async (title, description, orderNumber) => {
  const { data, error } = await supabase
    .from('sections')
    .insert({
      title,
      description,
      order_number: orderNumber,
    })
    .select()
    .single();
  
  return { data, error };
};

// Admin: Create lesson
export const createLesson = async (lessonData) => {
  const { data, error } = await supabase
    .from('lessons')
    .insert(lessonData)
    .select()
    .single();
  
  return { data, error };
};

// Admin: Upload video
export const uploadVideo = async (file, fileName) => {
  const { data, error } = await supabase.storage
    .from('videos')
    .upload(fileName, file);
  
  if (error) return { data: null, error };
  
  const { data: urlData } = supabase.storage
    .from('videos')
    .getPublicUrl(fileName);
  
  return { data: urlData.publicUrl, error: null };
};

// Admin: Upload attachment
export const uploadAttachment = async (file, lessonId) => {
  const fileName = `${lessonId}/${Date.now()}_${file.name}`;
  
  const { data, error } = await supabase.storage
    .from('attachments')
    .upload(fileName, file);
  
  if (error) return { data: null, error };
  
  const { data: urlData } = supabase.storage
    .from('attachments')
    .getPublicUrl(fileName);
  
  const { data: attachmentData, error: attachmentError } = await supabase
    .from('lesson_attachments')
    .insert({
      lesson_id: lessonId,
      file_name: file.name,
      file_url: urlData.publicUrl,
      file_type: file.type,
      file_size: file.size,
    })
    .select()
    .single();
  
  return { data: attachmentData, error: attachmentError };
};
```

---

#### 4. خدمات الكوبونات:

**src/services/couponService.js:**
```javascript
import { supabase } from './supabaseClient';

// Validate and apply coupon
export const applyCoupon = async (code, userId) => {
  // Get coupon
  const { data: coupon, error } = await supabase
    .from('coupons')
    .select('*')
    .eq('code', code.toUpperCase())
    .eq('is_active', true)
    .single();
  
  if (error) return { valid: false, message: 'الكود غير صحيح' };
  
  // Check expiry
  if (coupon.expires_at && new Date(coupon.expires_at) < new Date()) {
    return { valid: false, message: 'الكود منتهي الصلاحية' };
  }
  
  // Check usage limit
  if (coupon.current_uses >= coupon.max_uses) {
    return { valid: false, message: 'تم استنفاد جميع الاستخدامات' };
  }
  
  // Check if user already used it
  const { data: usage } = await supabase
    .from('coupon_usage')
    .select('*')
    .eq('coupon_id', coupon.id)
    .eq('user_id', userId)
    .single();
  
  if (usage) {
    return { valid: false, message: 'تم استخدام هذا الكود مسبقاً' };
  }
  
  return { valid: true, coupon };
};

// Record coupon usage
export const useCoupon = async (couponId, userId) => {
  // Insert usage record
  const { error: usageError } = await supabase
    .from('coupon_usage')
    .insert({
      coupon_id: couponId,
      user_id: userId,
    });
  
  if (usageError) return { error: usageError };
  
  // Increment current_uses
  const { error: updateError } = await supabase.rpc('increment_coupon_usage', {
    coupon_id: couponId
  });
  
  return { error: updateError };
};

// Admin: Create coupon
export const createCoupon = async (couponData) => {
  const { data, error } = await supabase
    .from('coupons')
    .insert({
      code: couponData.code.toUpperCase(),
      discount_type: couponData.discountType,
      discount_value: couponData.discountValue,
      max_uses: couponData.maxUses,
      expires_at: couponData.expiresAt,
      is_active: couponData.isActive,
    })
    .select()
    .single();
  
  return { data, error };
};

// Admin: Get all coupons
export const getAllCoupons = async () => {
  const { data, error } = await supabase
    .from('coupons')
    .select('*')
    .order('created_at', { ascending: false });
  
  return { data, error };
};
```

**SQL Function للـ increment:**
```sql
CREATE OR REPLACE FUNCTION increment_coupon_usage(coupon_id UUID)
RETURNS VOID AS $$
BEGIN
  UPDATE coupons
  SET current_uses = current_uses + 1
  WHERE id = coupon_id;
END;
$$ LANGUAGE plpgsql;
```

---

### المرحلة 4: بناء الصفحات

#### صفحة التسجيل (RegisterPage.jsx):
```javascript
import React, { useState } from 'react';
import { useNavigate, Link } from 'react-router-dom';
import { useAuth } from '../context/AuthContext';
import { toast } from 'react-toastify';

const RegisterPage = () => {
  const [formData, setFormData] = useState({
    fullName: '',
    email: '',
    password: '',
    confirmPassword: '',
  });
  const [loading, setLoading] = useState(false);
  const { signUp } = useAuth();
  const navigate = useNavigate();

  const handleChange = (e) => {
    setFormData({ ...formData, [e.target.name]: e.target.value });
  };

  const handleSubmit = async (e) => {
    e.preventDefault();
    
    if (formData.password !== formData.confirmPassword) {
      toast.error('كلمات المرور غير متطابقة');
      return;
    }

    setLoading(true);
    const { error } = await signUp(
      formData.email,
      formData.password,
      formData.fullName
    );

    if (error) {
      toast.error(error.message);
    } else {
      toast.success('تم التسجيل بنجاح! تحقق من بريدك الإلكتروني');
      navigate('/courses');
    }
    setLoading(false);
  };

  return (
    <div className="min-h-screen flex items-center justify-center bg-light-bg dark:bg-dark-bg">
      <div className="max-w-md w-full bg-white dark:bg-dark-secondary p-8 rounded-lg shadow-lg">
        <h2 className="text-3xl font-bold text-center mb-6 text-gray-900 dark:text-white">
          إنشاء حساب جديد
        </h2>
        
        <form onSubmit={handleSubmit} className="space-y-4">
          <div>
            <label className="block text-sm font-medium mb-2 dark:text-gray-300">
              الاسم الكامل
            </label>
            <input
              type="text"
              name="fullName"
              value={formData.fullName}
              onChange={handleChange}
              required
              className="w-full px-4 py-2 border rounded-lg focus:ring-2 focus:ring-primary dark:bg-dark-bg dark:border-gray-700 dark:text-white"
            />
          </div>

          <div>
            <label className="block text-sm font-medium mb-2 dark:text-gray-300">
              البريد الإلكتروني
            </label>
            <input
              type="email"
              name="email"
              value={formData.email}
              onChange={handleChange}
              required
              className="w-full px-4 py-2 border rounded-lg focus:ring-2 focus:ring-primary dark:bg-dark-bg dark:border-gray-700 dark:text-white"
            />
          </div>

          <div>
            <label className="block text-sm font-medium mb-2 dark:text-gray-300">
              كلمة المرور
            </label>
            <input
              type="password"
              name="password"
              value={formData.password}
              onChange={handleChange}
              required
              minLength={8}
              className="w-full px-4 py-2 border rounded-lg focus:ring-2 focus:ring-primary dark:bg-dark-bg dark:border-gray-700 dark:text-white"
            />
          </div>

          <div>
            <label className="block text-sm font-medium mb-2 dark:text-gray-300">
              تأكيد كلمة المرور
            </label>
            <input
              type="password"
              name="confirmPassword"
              value={formData.confirmPassword}
              onChange={handleChange}
              required
              className="w-full px-4 py-2 border rounded-lg focus:ring-2 focus:ring-primary dark:bg-dark-bg dark:border-gray-700 dark:text-white"
            />
          </div>

          <button
            type="submit"
            disabled={loading}
            className="w-full bg-primary hover:bg-primary-light text-white font-bold py-3 rounded-lg transition disabled:opacity-50"
          >
            {loading ? 'جاري التسجيل...' : 'إنشاء الحساب'}
          </button>
        </form>

        <p className="text-center mt-4 text-sm dark:text-gray-400">
          لديك حساب بالفعل؟{' '}
          <Link to="/login" className="text-primary hover:underline">
            تسجيل الدخول
          </Link>
        </p>
      </div>
    </div>
  );
};

export default RegisterPage;
```

---

### المرحلة 5: النشر على Vercel

#### 1. إعداد Git:
```bash
git init
git add .
git commit -m "Initial commit - Blaze Academy"
```

#### 2. رفع على GitHub:
```bash
# إنشاء repository على GitHub أولاً
git remote add origin https://github.com/yourusername/blaze-academy.git
git push -u origin main
```

#### 3. النشر على Vercel:

**الطريقة الأولى - عبر Dashboard:**
1. الذهاب إلى [vercel.com](https://vercel.com)
2. تسجيل الدخول بحساب GitHub
3. "New Project" → اختيار Repository
4. إضافة Environment Variables:
   - `REACT_APP_SUPABASE_URL`
   - `REACT_APP_SUPABASE_ANON_KEY`
5. Deploy

**الطريقة الثانية - عبر CLI:**
```bash
npm install -g vercel
vercel login
vercel
```

#### ملف vercel.json:
```json
{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "build"
      }
    }
  ],
  "routes": [
    {
      "src": "/static/(.*)",
      "dest": "/static/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
```

---

## 📱 التوافق مع الأجهزة (Responsive Design)

### Breakpoints:
- **Mobile:** < 640px
- **Tablet:** 640px - 1024px
- **Desktop:** > 1024px

### أمثلة Tailwind Responsive:
```jsx
<div className="
  flex flex-col md:flex-row
  p-4 md:p-8
  text-sm md:text-base lg:text-lg
">
  ...
</div>
```

---

## 🔔 الإشعارات (Notifications)

### استخدام React Toastify:

**في App.jsx:**
```javascript
import { ToastContainer } from 'react-toastify';
import 'react-toastify/dist/ReactToastify.css';

function App() {
  return (
    <>
      <Router>
        {/* Routes */}
      </Router>
      <ToastContainer
        position="top-left"
        rtl={true}
        theme="colored"
      />
    </>
  );
}
```

**في المكونات:**
```javascript
import { toast } from 'react-toastify';

// Success
toast.success('تم الحفظ بنجاح!');

// Error
toast.error('حدث خطأ، حاول مرة أخرى');

// Info
toast.info('معلومة مهمة');

// Warning
toast.warning('تحذير!');
```

---

## 📧 إرسال البريد الإلكتروني

### استخدام Supabase Email:

#### 1. إعداد Email Templates في Supabase:

**Confirmation Email:**
```html
<h2>مرحباً {{ .Name }}!</h2>
<p>شكراً لانضمامك إلى Blaze Academy</p>
<p>انقر على الرابط لتأكيد بريدك الإلكتروني:</p>
<a href="{{ .ConfirmationURL }}">تأكيد البريد</a>
```

**Subscription Confirmation:**
```html
<h2>تهانينا {{ .Name }}!</h2>
<p>تم تفعيل اشتراكك بنجاح في كورس "ميتا كما لم تراها من قبل"</p>
<p>يمكنك الآن الوصول إلى جميع الدروس</p>
<a href="https://your-site.vercel.app/courses">ابدأ التعلم</a>
```

#### 2. إرسال البريد عند الاشتراك:

**في paymentService.js:**
```javascript
export const completeSubscription = async (userId, amount, couponCode) => {
  // Update user subscription status
  const { error: userError } = await supabase
    .from('users')
    .update({
      is_subscribed: true,
      subscription_date: new Date().toISOString(),
    })
    .eq('id', userId);
  
  if (userError) return { error: userError };
  
  // Record subscription
  const { error: subError } = await supabase
    .from('subscriptions')
    .insert({
      user_id: userId,
      amount_paid: amount,
      coupon_code: couponCode,
      payment_status: 'completed',
    });
  
  // TODO: Send email notification
  // This can be done via Supabase Edge Functions or external service
  
  return { error: subError };
};
```

---

## 🎓 شهادة الإتمام (Certificate)

### عند إتمام 100%:

**في ProfilePage.jsx:**
```javascript
const CertificateSection = ({ progress }) => {
  if (progress < 100) {
    return (
      <div className="bg-white dark:bg-dark-secondary p-6 rounded-lg">
        <h3 className="text-xl font-bold mb-4">🎓 الشهادة</h3>
        <p className="text-gray-600 dark:text-gray-400 mb-4">
          أكمل الكورس للحصول على شهادتك!
        </p>
        <div className="w-full bg-gray-200 rounded-full h-4">
          <div
            className="bg-primary h-4 rounded-full"
            style={{ width: `${progress}%` }}
          />
        </div>
        <p className="text-center mt-2 text-sm">{progress}%</p>
      </div>
    );
  }

  return (
    <div className="bg-gradient-to-r from-primary to-primary-light p-6 rounded-lg text-white">
      <h3 className="text-2xl font-bold mb-4">🎉 تهانينا!</h3>
      <p className="mb-4">لقد أتممت الكورس بنجاح</p>
      <button className="bg-white text-primary px-6 py-2 rounded-lg font-bold hover:bg-gray-100">
        📥 تحميل الشهادة
      </button>
    </div>
  );
};
```

---

## 📊 لوحة الإحصائيات (Analytics)

### في Admin Dashboard:

```javascript
const DashboardStats = () => {
  const [stats, setStats] = useState({
    totalUsers: 0,
    subscribedUsers: 0,
    totalSections: 0,
    totalLessons: 0,
    activeCoupons: 0,
    revenue: 0,
  });

  useEffect(() => {
    fetchStats();
  }, []);

  const fetchStats = async () => {
    // Get user counts
    const { count: totalUsers } = await supabase
      .from('users')
      .select('*', { count: 'exact', head: true });

    const { count: subscribedUsers } = await supabase
      .from('users')
      .select('*', { count: 'exact', head: true })
      .eq('is_subscribed', true);

    // Get content counts
    const { count: totalSections } = await supabase
      .from('sections')
      .select('*', { count: 'exact', head: true });

    const { count: totalLessons } = await supabase
      .from('lessons')
      .select('*', { count: 'exact', head: true });

    // Get active coupons
    const { count: activeCoupons } = await supabase
      .from('coupons')
      .select('*', { count: 'exact', head: true })
      .eq('is_active', true);

    // Calculate revenue
    const { data: subscriptions } = await supabase
      .from('subscriptions')
      .select('amount_paid')
      .eq('payment_status', 'completed');

    const revenue = subscriptions?.reduce((sum, sub) => sum + sub.amount_paid, 0) || 0;

    setStats({
      totalUsers,
      subscribedUsers,
      totalSections,
      totalLessons,
      activeCoupons,
      revenue,
    });
  };

  return (
    <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      <StatCard icon="👥" title="إجمالي المستخدمين" value={stats.totalUsers} />
      <StatCard icon="💰" title="المشتركين" value={stats.subscribedUsers} />
      <StatCard icon="📚" title="الأقسام" value={stats.totalSections} />
      <StatCard icon="🎬" title="الدروس" value={stats.totalLessons} />
      <StatCard icon="🎟️" title="الكوبونات النشطة" value={stats.activeCoupons} />
      <StatCard icon="💵" title="الإيرادات" value={`${stats.revenue.toLocaleString()} د.ع`} />
    </div>
  );
};
```

---

## 🔒 الأمان (Security)

### Best Practices:

1. **تشفير البيانات:**
   - استخدام HTTPS
   - تشفير كلمات المرور (Supabase Auth)

2. **Row Level Security:**
   - تطبيق RLS على جميع الجداول
   - منع الوصول غير المصرح

3. **التحقق من الصلاحيات:**
   ```javascript
   const isAdmin = async (userId) => {
     const { data } = await supabase
       .from('users')
       .select('role')
       .eq('id', userId)
       .single();
     
     return data?.role === 'admin';
   };
   ```

4. **حماية الملفات:**
   - Storage Policies
   - توقيع URLs للملفات الحساسة

5. **Rate Limiting:**
   - Supabase يوفر rate limiting تلقائي
   - يمكن إضافة middleware إضافي

---

## 📈 التحسينات المستقبلية

### المرحلة التالية:

1. **نظام التعليقات:**
   - إضافة تعليقات على الدروس
   - نقاشات بين الطلاب

2. **الاختبارات (Quizzes):**
   - اختبار في نهاية كل قسم
   - النجاح مطلوب للمتابعة

3. **نظام النقاط والجوائز:**
   - نقاط لكل درس مكتمل
   - شارات (Badges)
   - لوحة المتصدرين

4. **Live Sessions:**
   - جلسات مباشرة مع المدرب
   - Q&A Sessions

5. **تطبيق الموبايل:**
   - React Native
   - نفس قاعدة البيانات

6. **تحليلات متقدمة:**
   - تتبع سلوك المستخدم
   - معدلات الإكمال
   - الدروس الأكثر مشاهدة

7. **نظام الإحالة (Referral):**
   - رابط إحالة لكل مستخدم
   - خصم عند إحالة صديق

8. **تكامل مع منصات الدفع:**
   - ZainCash API
   - Fastpay
   - بطاقات الائتمان

---

## 🛟 الدعم الفني

### قنوات الدعم:

1. **داخل الموقع:**
   - نموذج اتصال
   - FAQ Page

2. **خارج الموقع:**
   - واتساب
   - تليجرام
   - البريد الإلكتروني

---

## 📋 Checklist للتنفيذ

### ✅ الإعداد الأولي:
- [ ] إنشاء مشروع React
- [ ] تثبيت جميع المكتبات
- [ ] إعداد Tailwind CSS
- [ ] إنشاء مشروع Supabase
- [ ] إعداد Environment Variables

### ✅ قاعدة البيانات:
- [ ] إنشاء جميع الجداول
- [ ] تطبيق Row Level Security
- [ ] إنشاء Storage Buckets
- [ ] تطبيق Storage Policies

### ✅ المكونات الأساسية:
- [ ] AuthContext
- [ ] ThemeContext
- [ ] Navbar & Footer
- [ ] Protected Routes

### ✅ الصفحات:
- [ ] Landing Page
- [ ] Register Page
- [ ] Login Page
- [ ] Course Page
- [ ] Lesson Page
- [ ] Pricing Page
- [ ] Profile Page
- [ ] Admin Dashboard

### ✅ المميزات:
- [ ] نظام التسجيل والدخول
- [ ] نظام الأقسام والدروس
- [ ] مشغل الفيديو
- [ ] نظام الكوبونات
- [ ] تتبع التقدم
- [ ] الشهادة
- [ ] Dark/Light Mode

### ✅ Admin:
- [ ] إدارة الأقسام
- [ ] إدارة الدروس
- [ ] رفع الفيديوهات والمرفقات
- [ ] إدارة الكوبونات
- [ ] إدارة المستخدمين

### ✅ النشر:
- [ ] اختبار شامل
- [ ] رفع على GitHub
- [ ] النشر على Vercel
- [ ] إعداد Domain (اختياري)

---

## 🎯 الخلاصة

هذه وثيقة شاملة لبناء **Blaze Academy** - منصة تعليمية احترافية لكورس التسويق على ميتا. المشروع يستخدم:

- ⚛️ **React** للواجهة
- 🔥 **Supabase** للباك إند
- 🚀 **Vercel** للاستضافة
- 🎨 **Tailwind** للتصميم

الوثيقة تغطي:
- ✅ هيكل المشروع بالكامل
- ✅ قاعدة البيانات والعلاقات
- ✅ جميع الصفحات والمميزات
- ✅ كود جاهز للتنفيذ
- ✅ خطوات النشر

---

**📞 للدعم والاستفسارات:**
- يمكنك العودة لهذه الوثيقة في أي وقت
- جميع الأكواد جاهزة للنسخ واللصق
- الوثيقة منظمة وسهلة المتابعة

**🔥 Blaze Academy - ميتا كما لم تراها من قبل!**

---

*آخر تحديث: نوفمبر 2025*
*الإصدار: 1.0*