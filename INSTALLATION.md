# 🚀 دليل تحميل وتشغيل Blaze Academy على حاسوبك
## Installation Guide for Blaze Academy

---

## المتطلبات الأساسية 📋

قبل البدء، تأكد من تثبيت:

1. **Node.js** (الإصدار 18 أو أحدث)
   - تحميل من: [https://nodejs.org](https://nodejs.org)
   - اختر نسخة **LTS** (الموصى بها)

2. **Git** (لاستنساخ المشروع)
   - تحميل من: [https://git-scm.com/downloads](https://git-scm.com/downloads)

3. **محرر نصوص** (اختياري لكن موصى به)
   - VS Code: [https://code.visualstudio.com](https://code.visualstudio.com)
   - أو أي محرر آخر تفضله

4. **حساب Supabase** (مجاني)
   - سجل في: [https://supabase.com](https://supabase.com)

---

## الخطوات التفصيلية 🔧

### الخطوة 1️⃣: تحميل المشروع من GitHub

افتح **Terminal** أو **Command Prompt** واكتب:

#### **على Windows:**
```bash
# افتح Command Prompt أو PowerShell

# اذهب إلى المجلد الذي تريد تحميل المشروع فيه
cd Desktop

# استنسخ المشروع
git clone https://github.com/mustafaraedwork/blaze-academy.git

# ادخل إلى مجلد المشروع
cd blaze-academy
```

#### **على Mac/Linux:**
```bash
# افتح Terminal

# اذهب إلى المجلد الذي تريد تحميل المشروع فيه
cd ~/Desktop

# استنسخ المشروع
git clone https://github.com/mustafaraedwork/blaze-academy.git

# ادخل إلى مجلد المشروع
cd blaze-academy
```

---

### الخطوة 2️⃣: تثبيت المكتبات المطلوبة

بعد الدخول لمجلد المشروع، قم بتثبيت جميع المكتبات:

```bash
npm install
```

**هذا سيأخذ من 2-5 دقائق حسب سرعة الإنترنت**

انتظر حتى ترى رسالة:
```
added XXX packages
```

---

### الخطوة 3️⃣: إعداد ملف البيئة (.env.local)

#### أ) نسخ ملف المثال:

```bash
# على Windows (Command Prompt)
copy .env.example .env.local

# على Windows (PowerShell)
Copy-Item .env.example .env.local

# على Mac/Linux
cp .env.example .env.local
```

#### ب) تعديل الملف:

افتح ملف `.env.local` بأي محرر نصوص وأضف بياناتك:

```env
NEXT_PUBLIC_SUPABASE_URL=https://zgsucbivjwzrbigxkgbd.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Inpnc3VjYml2and6cmJpZ3hrZ2JkIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjIyMzEzMjIsImV4cCI6MjA3NzgwNzMyMn0.-dv_UdFPbc601djTt_zGGwr92SU43AtPiXNrjeZ9aEE
```

**احفظ الملف بعد التعديل!**

---

### الخطوة 4️⃣: إعداد قاعدة البيانات في Supabase

#### أ) افتح لوحة تحكم Supabase:
1. اذهب إلى: [https://supabase.com/dashboard](https://supabase.com/dashboard)
2. سجل دخول بحسابك
3. اختر مشروعك (أو أنشئ مشروع جديد إذا لم يكن لديك)

#### ب) افتح SQL Editor:
1. من القائمة الجانبية، اضغط على **"SQL Editor"** 📝
2. اضغط على **"New Query"** (استعلام جديد)

#### ج) نفذ سكريبت قاعدة البيانات:
1. افتح ملف `supabase-setup.sql` من مجلد المشروع
2. **انسخ كل محتوى الملف**
3. الصقه في SQL Editor
4. اضغط **"Run"** أو اضغط `Ctrl+Enter` (على Windows) أو `Cmd+Enter` (على Mac)

**انتظر حتى ترى:** ✅ Success. No rows returned

هذا يعني أن الجداول تم إنشاؤها بنجاح!

---

### الخطوة 5️⃣: التأكد من إعداد المشروع

تأكد من أن كل شيء جاهز:

```bash
# تحقق من Node.js
node -v
# يجب أن يظهر: v18.x.x أو أحدث

# تحقق من npm
npm -v
# يجب أن يظهر: 9.x.x أو أحدث

# تأكد من أن node_modules موجودة
ls node_modules
# على Windows
dir node_modules
```

---

### الخطوة 6️⃣: تشغيل المشروع! 🚀

الآن كل شيء جاهز! شغل المشروع:

```bash
npm run dev
```

**انتظر حتى ترى:**
```
✓ Ready in X ms
○ Local: http://localhost:3000
```

---

## 🎉 المشروع يعمل الآن!

افتح متصفحك واذهب إلى:
```
http://localhost:3000
```

يجب أن ترى الصفحة الرئيسية لـ Blaze Academy! 🎓

---

## اختبار تسجيل الدخول 🔐

### 1. إنشاء حساب جديد:
- اذهب إلى: [http://localhost:3000/signup](http://localhost:3000/signup)
- املأ النموذج:
  - **الاسم:** Test User
  - **البريد:** test@example.com
  - **كلمة المرور:** Test123456
- اضغط "تسجيل"

### 2. تسجيل الدخول:
- اذهب إلى: [http://localhost:3000/login](http://localhost:3000/login)
- سجل دخول بنفس البيانات:
  - **البريد:** test@example.com
  - **كلمة المرور:** Test123456
- اضغط "تسجيل الدخول"

**يجب أن يتم توجيهك إلى صفحة الملف الشخصي!** ✅

---

## الصفحات المتاحة للاختبار 📱

| الصفحة | الرابط |
|--------|--------|
| الرئيسية | `http://localhost:3000/` |
| من نحن | `http://localhost:3000/about` |
| تواصل معنا | `http://localhost:3000/contact` |
| تسجيل دخول | `http://localhost:3000/login` |
| إنشاء حساب | `http://localhost:3000/signup` |
| الملف الشخصي | `http://localhost:3000/profile` (يحتاج تسجيل دخول) |
| الدورات | `http://localhost:3000/courses` |

---

## حل المشاكل الشائعة 🔧

### ❌ المشكلة: "npm: command not found"
**الحل:** لم يتم تثبيت Node.js بشكل صحيح
- حمل Node.js من [nodejs.org](https://nodejs.org)
- أعد تشغيل Terminal بعد التثبيت

---

### ❌ المشكلة: "git: command not found"
**الحل:** لم يتم تثبيت Git
- حمل Git من [git-scm.com](https://git-scm.com)
- أعد تشغيل Terminal بعد التثبيت

---

### ❌ المشكلة: "port 3000 is already in use"
**الحل:** البورت 3000 مستخدم من برنامج آخر

**الخيار 1:** أوقف البرنامج الآخر

**الخيار 2:** استخدم بورت مختلف:
```bash
# على Windows
set PORT=3001 && npm run dev

# على Mac/Linux
PORT=3001 npm run dev
```

---

### ❌ المشكلة: "Module not found" أو "Cannot find module"
**الحل:** المكتبات لم يتم تثبيتها بشكل صحيح
```bash
# احذف node_modules والملف المقفل
rm -rf node_modules package-lock.json

# على Windows
rmdir /s node_modules
del package-lock.json

# ثبت من جديد
npm install
```

---

### ❌ المشكلة: "Failed to fetch" عند تسجيل الدخول
**الحل:** ملف `.env.local` غير موجود أو القيم خاطئة

**تحقق:**
1. هل ملف `.env.local` موجود في مجلد المشروع؟
2. هل القيم داخله صحيحة؟
3. هل أعدت تشغيل السيرفر بعد إنشاء الملف؟

**الحل:**
```bash
# أوقف السيرفر بـ Ctrl+C
# ثم شغله من جديد
npm run dev
```

---

### ❌ المشكلة: "Invalid login credentials"
**الحل:**
1. تأكد من تنفيذ `supabase-setup.sql` في Supabase
2. تأكد من إنشاء الحساب أولاً عبر `/signup`
3. تحقق من صحة البريد وكلمة المرور

---

### ❌ المشكلة: الصفحة تظهر "404 Not Found"
**الحل:** تأكد من:
1. السيرفر يعمل (يظهر `✓ Ready in...`)
2. الرابط صحيح: `http://localhost:3000` (وليس https)
3. أعد تحميل الصفحة بـ Ctrl+F5

---

## إيقاف المشروع 🛑

لإيقاف السيرفر:
```bash
# اضغط Ctrl+C في Terminal
# أو أغلق Terminal مباشرة
```

---

## تحديث المشروع 🔄

إذا تم تحديث المشروع على GitHub:

```bash
# احفظ أي تغييرات محلية أولاً
git stash

# اسحب آخر التحديثات
git pull origin main

# أو من فرع معين
git pull origin claude/review-content-011CUrC1RmS8apqgdEzJuxsW

# ثبت المكتبات الجديدة (إذا تمت إضافة أي منها)
npm install

# شغل المشروع
npm run dev
```

---

## بنية المشروع 📁

```
blaze-academy/
├── app/                    # صفحات Next.js
│   ├── page.tsx           # الصفحة الرئيسية
│   ├── login/             # صفحة تسجيل الدخول
│   ├── signup/            # صفحة التسجيل
│   ├── profile/           # الملف الشخصي
│   └── ...
├── components/            # مكونات React
├── contexts/              # Context API
├── lib/                   # مكتبات ووظائف مساعدة
├── public/                # ملفات ثابتة
├── .env.local            # متغيرات البيئة (محلي)
├── .env.example          # مثال لمتغيرات البيئة
├── supabase-setup.sql    # سكريبت قاعدة البيانات
├── SETUP.md              # دليل الإعداد
├── INSTALLATION.md       # هذا الملف
├── package.json          # معلومات المشروع
└── README.md             # الوصف العام
```

---

## أوامر مفيدة 💻

```bash
# تشغيل في وضع التطوير
npm run dev

# بناء المشروع للإنتاج
npm run build

# تشغيل النسخة المبنية
npm start

# فحص الأكواد
npm run lint

# تنظيف الكاش
rm -rf .next
# على Windows: rmdir /s .next
```

---

## نصائح للمطورين 👨‍💻

1. **استخدم VS Code** للحصول على تجربة أفضل
   - ثبت إضافات: ES7+ React/Redux, Tailwind CSS IntelliSense

2. **افتح Developer Tools** في المتصفح (`F12`)
   - راقب Console للأخطاء
   - استخدم Network لمراقبة الطلبات

3. **اقرأ رسائل الأخطاء** في Terminal
   - معظم الأخطاء تحتوي على الحل مباشرة

4. **راجع التوثيق:**
   - Next.js: [https://nextjs.org/docs](https://nextjs.org/docs)
   - Supabase: [https://supabase.com/docs](https://supabase.com/docs)
   - Tailwind: [https://tailwindcss.com/docs](https://tailwindcss.com/docs)

---

## الدعم والمساعدة 🆘

إذا واجهت أي مشاكل:

1. **راجع قسم "حل المشاكل الشائعة"** أعلاه
2. **افحص Console** في المتصفح والـ Terminal
3. **تأكد من اتباع جميع الخطوات** بالترتيب
4. **اقرأ ملف `SETUP.md`** لمزيد من التفاصيل

---

## ملاحظات مهمة ⚠️

1. **لا ترفع ملف `.env.local` لـ Git أبداً!**
   - يحتوي على مفاتيح سرية

2. **أعد تشغيل السيرفر** بعد:
   - تعديل `.env.local`
   - تثبيت مكتبات جديدة
   - تعديل ملفات التكوين

3. **استخدم HTTPS في Production فقط**
   - في Development استخدم HTTP عادي

4. **النسخة التجريبية:**
   - قاعدة البيانات تحتوي على بيانات تجريبية
   - يمكنك حذفها من Supabase Dashboard

---

## 🎊 مبروك!

أنت الآن تعمل على مشروع Blaze Academy محلياً!

**استمتع بالتطوير!** 🚀

---

**آخر تحديث:** 2025-06-11
**الإصدار:** 1.0.0
**الترخيص:** MIT
