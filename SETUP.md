# إعداد مشروع Blaze Academy 🚀
## Blaze Academy Setup Guide

---

## المتطلبات الأساسية | Prerequisites

- Node.js 18+
- npm أو yarn
- حساب Supabase (مجاني)

---

## خطوات الإعداد | Setup Steps

### 1️⃣ تثبيت المكتبات | Install Dependencies

```bash
npm install
# أو | or
yarn install
```

---

### 2️⃣ إعداد Supabase | Configure Supabase

#### أ) إنشاء مشروع Supabase:
1. انتقل إلى [https://supabase.com](https://supabase.com)
2. أنشئ حساب جديد أو سجل دخول
3. اضغط على "New Project"
4. اختر اسم المشروع وكلمة مرور قاعدة البيانات
5. انتظر حتى يتم إنشاء المشروع (حوالي دقيقتين)

#### ب) الحصول على بيانات الاتصال:
1. من لوحة تحكم Supabase، اذهب إلى **Settings** → **API**
2. انسخ القيمتين التاليتين:
   - **Project URL** (تبدأ بـ `https://xxxxx.supabase.co`)
   - **anon/public key** (مفتاح طويل يبدأ بـ `eyJhbG...`)

#### ج) إنشاء ملف البيئة:
1. انسخ ملف `.env.example` وأعد تسميته إلى `.env.local`:
```bash
cp .env.example .env.local
```

2. افتح ملف `.env.local` وأضف قيمك:
```env
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
```

---

### 3️⃣ إنشاء قاعدة البيانات | Create Database Tables

انتقل إلى **SQL Editor** في Supabase Dashboard وقم بتنفيذ الكود التالي:

```sql
-- جدول الملفات الشخصية | Profiles table
CREATE TABLE IF NOT EXISTS profiles (
  id UUID REFERENCES auth.users(id) PRIMARY KEY,
  name TEXT NOT NULL,
  email TEXT UNIQUE NOT NULL,
  avatar_url TEXT,
  bio TEXT,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- تفعيل Row Level Security
ALTER TABLE profiles ENABLE ROW LEVEL SECURITY;

-- السماح للمستخدمين بقراءة ملفاتهم الشخصية
CREATE POLICY "Users can view own profile"
  ON profiles FOR SELECT
  USING (auth.uid() = id);

-- السماح للمستخدمين بتحديث ملفاتهم الشخصية
CREATE POLICY "Users can update own profile"
  ON profiles FOR UPDATE
  USING (auth.uid() = id);

-- السماح بإنشاء ملف شخصي عند التسجيل
CREATE POLICY "Users can insert own profile"
  ON profiles FOR INSERT
  WITH CHECK (auth.uid() = id);
```

---

### 4️⃣ تشغيل المشروع | Run the Project

#### Development Mode:
```bash
npm run dev
# أو | or
yarn dev
```

المشروع سيعمل على: [http://localhost:3000](http://localhost:3000)

#### Production Build:
```bash
npm run build
npm start
```

---

## اختبار تسجيل الدخول | Testing Login

### إنشاء حساب تجريبي:
1. اذهب إلى: [http://localhost:3000/signup](http://localhost:3000/signup)
2. املأ النموذج بالبيانات التالية:
   - الاسم: Test User
   - البريد الإلكتروني: test@example.com
   - كلمة المرور: Test123456

3. اضغط "تسجيل"

### تسجيل الدخول:
1. اذهب إلى: [http://localhost:3000/login](http://localhost:3000/login)
2. استخدم نفس البيانات:
   - البريد: test@example.com
   - كلمة المرور: Test123456

3. يجب أن يتم توجيهك إلى صفحة الملف الشخصي `/profile`

---

## حل المشاكل الشائعة | Troubleshooting

### ❌ "Failed to fetch" أو خطأ في الاتصال
**السبب:** متغيرات البيئة غير صحيحة أو غير موجودة

**الحل:**
1. تأكد من وجود ملف `.env.local`
2. تحقق من صحة `NEXT_PUBLIC_SUPABASE_URL` و `NEXT_PUBLIC_SUPABASE_ANON_KEY`
3. أعد تشغيل السيرفر بعد تعديل ملف `.env.local`

---

### ❌ "Invalid login credentials"
**السبب:** البريد الإلكتروني أو كلمة المرور غير صحيحة

**الحل:**
1. تأكد من تسجيل الحساب أولاً عبر `/signup`
2. تأكد من صحة البريد الإلكتروني وكلمة المرور
3. تحقق من تفعيل البريد الإلكتروني إذا كانت هذه الميزة مفعلة في Supabase

---

### ❌ "profiles table does not exist"
**السبب:** لم يتم إنشاء جداول قاعدة البيانات

**الحل:**
1. اذهب إلى Supabase Dashboard → SQL Editor
2. قم بتنفيذ كود SQL الموجود في الخطوة 3️⃣ أعلاه
3. حاول التسجيل مرة أخرى

---

### ❌ الزر يبقى في حالة تحميل
**السبب:** تم إصلاح هذه المشكلة في آخر تحديث

**الحل:**
1. تأكد من أن لديك آخر نسخة من الكود
2. إذا استمرت المشكلة، تحقق من console في المتصفح للأخطاء
3. تأكد من صحة متغيرات البيئة

---

## الصفحات المتاحة | Available Pages

- `/` - الصفحة الرئيسية | Home
- `/about` - من نحن | About
- `/contact` - تواصل معنا | Contact
- `/login` - تسجيل الدخول | Login
- `/signup` - إنشاء حساب | Signup
- `/profile` - الملف الشخصي | Profile (محمي | Protected)
- `/courses` - الدورات | Courses
- `/watch/[lessonId]` - مشاهدة الدروس | Watch Lessons (محمي | Protected)

---

## الأوامر المتاحة | Available Commands

```bash
npm run dev      # تشغيل السيرفر في وضع التطوير
npm run build    # بناء المشروع للإنتاج
npm start        # تشغيل المشروع في وضع الإنتاج
npm run lint     # فحص الكود بـ ESLint
```

---

## دعم | Support

إذا واجهت أي مشاكل:
1. تحقق من console المتصفح للأخطاء
2. تحقق من terminal للأخطاء في السيرفر
3. راجع ملف `Blaze_Academy.md` للتوثيق الكامل
4. تأكد من اتباع جميع خطوات الإعداد أعلاه

---

## ملاحظات مهمة | Important Notes

⚠️ **ملف `.env.local` يجب أن لا يتم رفعه لـ Git**
- تم إضافته بالفعل إلى `.gitignore`
- لا تشارك مفاتيح Supabase الخاصة بك مع أحد

✅ **التغييرات الأخيرة:**
- إصلاح مشكلة loading في صفحة تسجيل الدخول
- استخدام AuthContext بدلاً من supabase مباشرة
- إضافة finally block لضمان إيقاف التحميل في جميع الحالات

---

🎉 **جاهز للانطلاق!** | **Ready to Go!**
